apiVersion: v1
kind: Secret
metadata:
  name: {{ include "image-service.fullname" . }}-secret
type: Opaque
data:
  mysql-root-password: {{ .Values.env.dbRootPassword }}
  mysql-user-password: {{ .Values.env.dbPassword }}
  minio-secret-key: {{ .Values.env.minioSecretKey }}